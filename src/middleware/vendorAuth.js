const {verifyToken}=require("../utils/jwt");const pool=require("../config/db");async function vendorAuth(req,res,next){try{const h=req.headers.authorization||"";const t=h.startsWith("Bearer ")?h.slice(7):null;if(!t)return res.status(401).json({error:"مطلوب تسجيل الدخول"});let p;try{p=verifyToken(t)}catch{return res.status(401).json({error:"رمز غير صالح"})}if(p.role!=="vendor")return res.status(403).json({error:"مخصص للمتاجر"});const {rows}=await pool.query("SELECT vendor_id,deleted_at FROM vendors WHERE vendor_id=$1",[p.sub]);const v=rows[0];if(!v||v.deleted_at)return res.status(403).json({error:"الحساب غير مفعل"});req.auth=p;next()}catch(e){next(e)}}module.exports=vendorAuth

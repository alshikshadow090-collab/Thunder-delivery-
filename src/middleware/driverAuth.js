const {verifyToken}=require("../utils/jwt");const pool=require("../config/db");async function driverAuth(req,res,next){try{const h=req.headers.authorization||"";const t=h.startsWith("Bearer ")?h.slice(7):null;if(!t)return res.status(401).json({error:"مطلوب تسجيل الدخول"});let p;try{p=verifyToken(t)}catch{return res.status(401).json({error:"رمز غير صالح"})}if(p.role!=="driver")return res.status(403).json({error:"مخصص للسائقين"});const {rows}=await pool.query("SELECT driver_id,is_active,deleted_at FROM drivers WHERE driver_id=$1",[p.sub]);const d=rows[0];if(!d||d.deleted_at||!d.is_active)return res.status(403).json({error:"الحساب غير مفعل"});req.auth=p;next()}catch(e){next(e)}}module.exports=driverAuth
